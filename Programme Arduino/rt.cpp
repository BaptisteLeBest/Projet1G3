#include "coeur.h"
#include "Arduino.h"
  
/*int valeurPrecedente = 0;
long tempsPrecedent = 0;
int Ledba ,cal;
int choix; 
int g = choix+1;
void rt()
{
  

  int valeurActuelle, valeurSeuil;
  long tempsDetection;
     if (Serial.available() > 0) 
  {
             choix = Serial.read()-48; 
             digitalWrite(g, HIGH); 
             delay(30);
             digitalWrite(g, LOW); 
  } 
  
 /* valeurActuelle = analogRead(0);
  valeurSeuil = 380;
  if (Serial.available() > 0) 
  {
     choix = Serial.read()-48;           
     if (valeurActuelle > valeurSeuil) 
      {  
        if (valeurPrecedente <= valeurSeuil) 
        {  
          tempsDetection = millis();
          if (tempsDetection > (tempsPrecedent + 200))
          {  
             Ledba = (1000.0 * 60.0) / (tempsDetection - tempsPrecedent),0;            
             digitalWrite(g, HIGH); 
             delay(30);
             digitalWrite(g, LOW);      
             cal  = (1000/Ledbat)*60;
             delay(cal);
             tempsPrecedent = tempsDetection;       
        
         }
        }
      }
  
  valeurPrecedente = valeurActuelle;
  }*/
//}
